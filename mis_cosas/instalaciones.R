# Instala el paquete
install.packages("tidyverse", dependencies = TRUE)
# Activa la biblioteca para poder utilizarla
library(tidyverse)

# Este paquete es para generar cuadrdaos mágicos
install.packages("magic", dependencies = TRUE)
library(magic)
# Calcular cuadrado mágico
magic(6)

# COnsultar paquetes instalados
installed.packages()
